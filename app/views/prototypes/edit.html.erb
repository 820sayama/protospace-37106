<div class="main">
  <div class="inner">
    <div class="form__wrapper">
      <h2 class="page-heading">プロトタイプ編集</h2>
     <span class="name">
     <a href="/users/<%= @prototype.user.id %>">
      <span>投稿者</span><%= @prototype.user.name %>
     </a>
    </span>
      <%# 部分テンプレートでフォームを表示する %>
      <%= render partial: 'form', locals: { prototype: @prototype } %>
    </div>
  </div>
</div>